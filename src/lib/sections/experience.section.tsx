import React from "react";
import { SectionHeader, SectionWrapper } from "$components";

export const Experience = () => {
  return (
    <SectionWrapper background="rgb(var(--primary-green))" id="experience">
      <SectionHeader index={4}>experience</SectionHeader>
    </SectionWrapper>
  );
};
