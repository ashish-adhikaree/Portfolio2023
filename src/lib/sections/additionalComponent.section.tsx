import React from "react";
import { SectionHeader, SectionWrapper } from "$components";

export const AdditionalComponent = () => {
  return (
    <SectionWrapper id="hireme">
      <SectionHeader green={true} index={5}>
        hire me
      </SectionHeader>
    </SectionWrapper>
  );
};
