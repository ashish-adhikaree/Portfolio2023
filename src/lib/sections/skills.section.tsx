import React from "react";
import { SectionHeader, SectionWrapper } from "$components";

export const Skills = () => {
  return (
    <SectionWrapper background="rgb(var(--primary-green))" id="skills">
      <SectionHeader index={2}>skills</SectionHeader>
    </SectionWrapper>
  );
};
