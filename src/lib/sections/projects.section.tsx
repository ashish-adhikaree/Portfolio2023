import React from "react";
import { SectionHeader, SectionWrapper } from "$components";

export const Projects = () => {
  return (
    <SectionWrapper id="projects">
      <SectionHeader green={true} index={3}>
        projects
      </SectionHeader>
    </SectionWrapper>
  );
};
